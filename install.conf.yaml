- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
  - ~/Code

- shell:
#  - [git submodule update --init --recursive, Installing submodules]
   -
    command: echo "Hey You Guys!"
    stdin: true
    stdout: true
    stderr: true
   - sleep 2
   - ./test.zsh
