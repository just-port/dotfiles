- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.vim: vim
    ~/.zshrc: zshrc

- create:
  - ~/Code
  - ~/Code/wut/up/dawg
# - shell:
#  - [git submodule update --init --recursive, Installing submodules]
